# Core dependencies for crawling system
requests>=2.28.0
tqdm>=4.65.0
beautifulsoup4>=4.11.0
lxml>=4.9.0
pyyaml>=6.0
trafilatura>=1.6.0

# Playwright for JS rendering
playwright>=1.40.0
playwright-stealth>=1.0.0

# For HTML parsing
html5lib>=1.1

# Advanced HTTP clients
httpx>=0.25.0
curl_cffi>=0.5.0

# Anti-bot evasion (all local, no external services)
undetected-chromedriver>=3.5.0
cloudscraper>=1.2.0
fake-useragent>=1.4.0
browserforge>=1.0.0

# Selenium with request interception
selenium>=4.15.0
selenium-wire>=5.1.0
